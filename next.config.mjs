/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Optional: if static export is needed
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
